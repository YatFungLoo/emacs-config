# Big Emacs Config 2025

Written for GNU Emacs 30.2 (build 1, aarch64-apple-darwin24.6.0) of 2025-12-10
from Homebrew.

Run `package-install-selected-packages` and `package-autoremove` to retrieve
packages.

`eglot` requires manual start up.
